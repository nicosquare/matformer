# Copy this file to `.env` and fill in real values before running training.

# Hugging Face cache root used by `python-dotenv` at startup.
HF_HOME=/mnt/experiments/hf-cache

# Hugging Face access token for private models or datasets.
HF_TOKEN=your_huggingface_token_here
