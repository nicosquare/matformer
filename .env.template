# Copy this file to the ignored `.env` file and fill in values privately.
# Do not commit identities, credentials, or site-specific filesystem paths.

# Required by the 10B operator commands.
TOKENIZER=
CORPUS=
OUT=

# Optional cache and credential settings. Prepared-corpus training does not
# require remote dataset access or Weights & Biases credentials.
HF_HOME=
HF_DATASETS_CACHE=
TRANSFORMERS_CACHE=
HF_TOKEN=
