[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "matformer"
version = "0.0.0"
description = "MatFormer reproduction experiments"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
